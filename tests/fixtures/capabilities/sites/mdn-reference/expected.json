{
  "title": "Array.prototype.map()",
  "word_count_min": 45,
  "must_contain": ["creates a new array", "const doubled", "does not change the original array"],
  "must_not_contain": ["Theme", "Related resources", "Web platform"],
  "html_must_contain": ["<code>map()</code>", "<pre><code"],
  "html_must_not_contain": ["top-header", "breadcrumbs"],
  "headings_min": 3,
  "code_blocks_min": 1
}
