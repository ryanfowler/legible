This article contains code blocks from several syntax highlighters. Each block has a reliable language hint in a different metadata source.

```rust
fn main() {}
```

```python
print("data-language")
```

```typescript
const typed: boolean = true;
```

```bash
printf '%s\n' "$HOME"
```

Language: CMake

```cmake
project(example)
```

The normalized blocks must produce fenced Markdown with the supplied language and unchanged source text.
