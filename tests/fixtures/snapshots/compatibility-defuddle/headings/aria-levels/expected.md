This reference uses explicit heading roles when native heading elements are unavailable.

## Configuration

Apply the configuration in a predictable order and validate the final output.

### Additional notes

The final heading remains available through its explicit ARIA level.
