The interface uses a rounded control[^1] while the article explains the design.

A normal aside must remain because it has no clear reference and definition relationship.

This aside contains useful related explanation rather than a sidenote.

[^1]: The control follows the current design system.
